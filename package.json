{
  "name": "checkin-console-cloud-v3",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev --remote",
    "db:migrate": "wrangler d1 migrations apply checkin_v3 --remote",
    "db:migrate:local": "wrangler d1 migrations apply checkin_v3 --local"
  },
  "devDependencies": {
    "wrangler": "^4.124.0"
  }
}
